import 'package:flutter/material.dart';

class DriverHomeWidget extends StatelessWidget {
  const DriverHomeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('driver'),
    );
  }
}
